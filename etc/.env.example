# MCP-Funnel Environment Variables

# HTTP server port (default: 3000)
PORT=3000

# Data storage directory (default: ./data locally, /data in Docker)
DATA_DIR=./data

# Session secret (auto-generated if not set)
# SESSION_SECRET=your-secret-here

# Session max age in ms (default: 30 days)
# SESSION_MAX_AGE=2592000000

# Auto-create admin on first start (optional)
# ADMIN_USER=admin
# ADMIN_PASS=your-password-here

# Log level (default: info)
LOG_LEVEL=info

# Node environment (default: production)
NODE_ENV=production
